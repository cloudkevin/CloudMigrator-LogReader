# CloudMigrator-LogReader
Search through CloudMigrator log files and extract each one containing the desired error

## Inputs

argv[1] = Path to the CloudMigrator log being parsed  
w = Value being searched for (default="[Warn]")
